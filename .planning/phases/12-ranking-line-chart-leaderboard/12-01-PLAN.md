---
phase: 12
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/test/setup.ts
autonomous: true
requirements: []
tags: [frontend, chart, recharts, preflight]

must_haves:
  truths:
    - "recharts@3.8.1 is installed as a dependency in frontend/package.json"
    - "Test environment provides a ResizeObserver implementation so Recharts ResponsiveContainer does not crash in jsdom"
    - "All existing tests still pass after the dependency and setup changes"
  artifacts:
    - path: "frontend/package.json"
      provides: "recharts dependency declaration at version ^3.8.1"
      contains: "\"recharts\""
    - path: "frontend/src/test/setup.ts"
      provides: "ResizeObserver polyfill for jsdom"
      contains: "ResizeObserver"
  key_links:
    - from: "frontend/src/test/setup.ts"
      to: "global.ResizeObserver"
      via: "class assignment with observe/unobserve/disconnect noops"
      pattern: "global\\.ResizeObserver\\s*=\\s*class"
---

<objective>
Wave-0 preflight for Phase 12: install recharts@3.8.1 and add the ResizeObserver mock that ResponsiveContainer needs in jsdom. Both downstream component plans (02 EloLineChart, 03 EloLeaderboard) and the integration plan (04) depend on this completing first.

Purpose: Phase 12 D-01 mandates recharts@3.8.1 and a 3-line ResizeObserver stub in `src/test/setup.ts`. Without this, vitest crashes the moment any test renders `<ResponsiveContainer>`. Doing it as Wave 0 keeps the parallel component plans (02, 03) clean — they only need to import from `recharts` and write tests.

Output:
- recharts@3.8.1 added to `frontend/package.json` `dependencies`
- `frontend/package-lock.json` regenerated to include recharts and its transitive deps
- `frontend/src/test/setup.ts` has a `global.ResizeObserver` class polyfill with `observe`, `unobserve`, `disconnect` no-op methods
- `npm run test` (existing tests) still passes — no regression from the polyfill addition
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ranking-line-chart-leaderboard/12-CONTEXT.md
@.planning/phases/12-ranking-line-chart-leaderboard/12-PATTERNS.md
@.planning/phases/12-ranking-line-chart-leaderboard/12-UI-SPEC.md

<interfaces>
<!-- Existing test setup pattern in src/test/setup.ts (lines 1-17) -->

```typescript
process.env.TZ = 'America/Argentina/Buenos_Aires'
import '@testing-library/jest-dom'

// Mock localStorage for jsdom environment
const localStorageMock = (() => {
  let store: Record<string, string> = {}
  return {
    getItem: (key: string) => store[key] ?? null,
    setItem: (key: string, value: string) => { store[key] = value },
    removeItem: (key: string) => { delete store[key] },
    clear: () => { store = {} },
    get length() { return Object.keys(store).length },
    key: (i: number) => Object.keys(store)[i] ?? null,
  }
})()

Object.defineProperty(window, 'localStorage', { value: localStorageMock, writable: true })
```

The new ResizeObserver mock follows the same "polyfill missing browser API" intent and lives in the same file, after the localStorage block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install recharts@3.8.1</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (confirm current dependencies block, react and react-dom are at ^18.3.1)
    - .planning/phases/12-ranking-line-chart-leaderboard/12-CONTEXT.md (D-01 — recharts@3.8.1 is the locked library decision)
  </read_first>
  <action>
    From the `frontend/` directory, run exactly:

    ```bash
    cd frontend && npm install recharts@3.8.1
    ```

    This must:
    - Add `"recharts": "^3.8.1"` (or `"3.8.1"` — whatever npm writes by default with this exact-version install) to the `dependencies` block of `frontend/package.json`. The CONTEXT decision D-01 is "recharts@3.8.1" — pin to that exact version.
    - Update `frontend/package-lock.json` with recharts and its transitive deps.
    - Not modify `react` or `react-dom` versions.

    Do NOT use `--save-dev` — recharts is a runtime dependency, not a dev dependency. Do NOT use `npm install` without a version (would float to latest, breaking D-01). Do NOT add to `devDependencies`.

    After install: run `cd frontend && npm ls recharts` to confirm it resolved to 3.8.1.
  </action>
  <verify>
    <automated>cd frontend && grep -q "\"recharts\":" package.json &amp;&amp; npm ls recharts 2>&amp;1 | grep -q "recharts@3.8.1"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains the literal string `"recharts":` inside the `dependencies` block (NOT `devDependencies`)
    - `cd frontend && npm ls recharts` output contains `recharts@3.8.1`
    - `frontend/package-lock.json` contains the literal string `"recharts"` (proves lockfile was regenerated)
    - `frontend/package.json` `react` field is still `^18.3.1` and `react-dom` is still `^18.3.1` (no accidental upgrades)
  </acceptance_criteria>
  <done>recharts@3.8.1 is in `frontend/package.json` dependencies, lockfile regenerated, no React version drift.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add ResizeObserver polyfill to test setup</name>
  <files>frontend/src/test/setup.ts</files>
  <read_first>
    - frontend/src/test/setup.ts (current 17 lines, must not break existing localStorage mock)
    - .planning/phases/12-ranking-line-chart-leaderboard/12-CONTEXT.md (D-01 — "mock global.ResizeObserver in src/test/setup.ts (3-line stub)")
    - .planning/phases/12-ranking-line-chart-leaderboard/12-PATTERNS.md (§ "frontend/src/test/setup.ts" — exact placement and code)
  </read_first>
  <action>
    Append the following ResizeObserver polyfill to `frontend/src/test/setup.ts`, AFTER the existing `Object.defineProperty(window, 'localStorage', ...)` line. Do not modify or delete any existing lines.

    Append exactly this block:

    ```typescript

    // Mock ResizeObserver so Recharts ResponsiveContainer doesn't crash in jsdom.
    // Required by recharts@3.8.1's <ResponsiveContainer> internal layout effect.
    // See .planning/phases/12-ranking-line-chart-leaderboard/12-CONTEXT.md D-01.
    global.ResizeObserver = class ResizeObserver {
      observe() {}
      unobserve() {}
      disconnect() {}
    }
    ```

    Notes:
    - Use `global.ResizeObserver = ...` (NOT `window.ResizeObserver`). vitest's jsdom env exposes both, but `global` matches what Recharts and any other library would do at module scope.
    - The class methods are intentionally empty — Recharts only checks for the API surface, not behavior.
    - Do NOT wrap in `if (!global.ResizeObserver)` — vitest jsdom does not provide one, and the unconditional assignment is the documented pattern.

    Do not add `vi.stubGlobal(...)` — the class assignment runs once at setup, before any test, which is exactly what's needed.
  </action>
  <verify>
    <automated>grep -q "global.ResizeObserver = class ResizeObserver" frontend/src/test/setup.ts &amp;&amp; grep -q "Object.defineProperty(window, 'localStorage'" frontend/src/test/setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/test/setup.ts` contains the literal substring `global.ResizeObserver = class ResizeObserver`
    - `frontend/src/test/setup.ts` contains all three method names: `observe()`, `unobserve()`, `disconnect()` (each on its own line inside the class body)
    - The pre-existing `Object.defineProperty(window, 'localStorage'` line is still present (no regressions)
    - `frontend/src/test/setup.ts` still has the `import '@testing-library/jest-dom'` line at the top (no order shuffle)
  </acceptance_criteria>
  <done>ResizeObserver class polyfill appended to setup.ts after localStorage mock; existing setup intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify existing test suite still green</name>
  <files></files>
  <read_first>
    - frontend/package.json (confirm `"test": "vitest"` script exists)
  </read_first>
  <action>
    Run the full frontend test suite to confirm Tasks 1 and 2 introduced no regressions:

    ```bash
    cd frontend && npm test -- --run
    ```

    Expected: every existing test (Login, AchievementCard, EloSummaryCard, Ranking, RankingFilters, etc. — see `frontend/src/test/components/`) still passes. The ResizeObserver polyfill adds a global, never removes one, so no test should break.

    If any test fails:
    - DO NOT modify the failing test
    - Re-read the failure output and identify whether it's caused by the polyfill (very unlikely — the polyfill is purely additive) or unrelated
    - If polyfill-caused: report exact error message and stop
    - If unrelated: still stop and report — Phase 12 should not paper over pre-existing breakage

    Use `--run` (not watch mode) so vitest exits with a clean status code.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm test -- --run</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm test -- --run` exits with code 0
    - Stdout ends with a `Test Files` summary where `failed` count is 0 and at least 13 test files passed (matches existing component test count)
    - No test output line contains the literal string `ResizeObserver is not defined` (proves polyfill is wired correctly)
  </acceptance_criteria>
  <done>Full vitest run is green after install + setup changes; downstream plans can proceed.</done>
</task>

</tasks>

<threat_model>
**ASVS Level:** 1
**Threats identified:**
- Supply-chain: pulling `recharts@3.8.1` from npm — mitigation: pin exact version per D-01 (no floating range), reviewed in 12-RESEARCH.md alternatives matrix; no install-time scripts in recharts package
- Test-environment global pollution: assigning `global.ResizeObserver` could mask a future real implementation — mitigation: scoped to `src/test/setup.ts` (test-only file, never bundled into runtime); the class is a typed no-op so it cannot cause information disclosure
- Lockfile drift: `npm install` could update unrelated transitive deps — mitigation: Task 1 explicitly verifies `react`/`react-dom` versions unchanged in acceptance criteria

**High severity threats:** none
</threat_model>

<verification>
1. `frontend/package.json` `dependencies.recharts` resolves to 3.8.1 (`npm ls recharts` confirms)
2. `frontend/src/test/setup.ts` defines `global.ResizeObserver` with `observe`, `unobserve`, `disconnect`
3. `npm test -- --run` exits 0 from the `frontend/` directory with all existing tests green
</verification>

<success_criteria>
- recharts@3.8.1 installed as a runtime dependency
- ResizeObserver polyfill present in test setup
- Pre-existing test suite still passes (no regression)
- Downstream plans 02, 03, 04 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/12-ranking-line-chart-leaderboard/12-01-SUMMARY.md` documenting:
- recharts version installed (resolved from `npm ls recharts`)
- Number of test files passing in the post-install run
- Any unexpected transitive dep changes observed in `package-lock.json` diff
</output>
</content>
</invoke>