---
phase: 07-documentacion-y-proceso
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-phases/01-backend-core/01-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/01-backend-core/01-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-integraci-n-y-api/02-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/02-integraci-n-y-api/02-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-frontend/03-01-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-frontend/03-02-SUMMARY.md
  - .planning/milestones/v1.0-phases/03-frontend/03-03-SUMMARY.md
  - .planning/milestones/v1.0-phases/04-reconciliador/04-01-SUMMARY.md
autonomous: true
requirements: []
gap_closure:
  - DOC-DRIFT-evaluator-count
  - PROCESS-summary-frontmatter-inconsistent
threat_model_severity: low
must_haves:
  truths:
    - "Las 8 SUMMARYs v1.0 archivadas en .planning/milestones/v1.0-phases/ contienen `requirements:` y `requirements-completed:` en su frontmatter top-level (no anidado dentro de dependency_graph)"
    - "01-02-SUMMARY.md ya no menciona '5 achievement definitions' — el conteo refleja los 12 evaluadores definidos en backend/services/achievement_evaluators/registry.py"
    - "02-02-SUMMARY.md ya no menciona 'all 5 achievements' ni 'all 5 achievement definitions' — el conteo refleja 12"
    - "Ningún cambio en las decisiones, deviations, o body de las SUMMARYs más allá de los strings de drift y la adición de frontmatter"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/01-backend-core/01-02-SUMMARY.md"
      provides: "SUMMARY del plan de evaluadores con frontmatter estandarizado y conteo correcto (12 definiciones)"
      not_contains: "5 achievement definitions"
    - path: ".planning/milestones/v1.0-phases/02-integraci-n-y-api/02-02-SUMMARY.md"
      provides: "SUMMARY de endpoints con frontmatter estandarizado y conteo correcto (12 logros)"
      not_contains: "all 5 achievement"
---

<objective>
Cerrar dos items de tech_debt cross-cutting del audit `v1.0-MILESTONE-AUDIT.md`:

1. **Doc drift de conteo de evaluadores:** `01-02-SUMMARY.md:44` y `02-02-SUMMARY.md` líneas 14–15 dicen "5" cuando hoy hay 12 definiciones registradas en `registry.py`.
2. **Frontmatter inconsistente:** sólo 1 de las 8 SUMMARYs v1.0 tiene `requirements:` y sólo 1 tiene `requirements-completed:` (en posición top-level del frontmatter, no anidado). Las otras 6 quedan sin trazabilidad estándar.

**Por qué:** El audit lo flagea explícitamente como tech_debt cross-cutting. Estandarizar el frontmatter permite escanear las SUMMARYs sin hacer cross-reference manual contra VERIFICATION.md tables.

**Mapeo plan → REQ-IDs (derivado del cuerpo de cada PLAN.md más la tabla Traceability de `.planning/milestones/v1.0-REQUIREMENTS.md`):**
- 01-01: PERS-01, PERS-02, PERS-03, PERS-04
- 01-02: CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-07, CORE-08
- 02-01: API-03, INTG-01, INTG-02, INTG-04, INTG-05
- 02-02: API-01, API-02, INTG-03
- 03-01: ENDG-02, ENDG-03, PROF-02, PROF-03, PROF-04, ICON-01, ICON-02, ICON-03
- 03-02: ENDG-01, PROF-01
- 03-03: CATL-01, CATL-02
- 04-01: TOOL-01, TOOL-02, TOOL-03
</objective>

<tasks>

<task type="auto">
  <name>Tarea 1: Backfill frontmatter en las 8 v1.0 SUMMARYs + fix drift de conteo</name>
  <files>(las 8 SUMMARYs listadas en files_modified)</files>
  <read_first>
    - Cada SUMMARY que se va a editar (verificar la posición exacta del frontmatter top-level — algunas tienen `dependency_graph.requires:` anidado que NO se debe confundir con el campo top-level `requirements:`)
    - El mapeo plan → REQ-IDs definido en `<objective>` arriba
  </read_first>
  <action>
    Para cada SUMMARY:

    1. Localizar el frontmatter (entre las primeras dos líneas `---`).
    2. Si **no existe** una línea top-level `requirements:` en el frontmatter, agregarla justo después del campo `tags:` (o en su defecto, después de `subsystem:`) con la lista de IDs del mapeo en `<objective>`. Formato YAML inline:
       ```
       requirements: [PERS-01, PERS-02, PERS-03, PERS-04]
       ```
    3. Si **no existe** una línea top-level `requirements-completed:`, agregarla inmediatamente después de `requirements:` con la **misma lista** (todos los REQs del plan se completaron).
    4. Si alguno de los dos campos ya existe en posición top-level: dejarlo intacto (idempotente).
    5. **No** modificar campos anidados como `dependency_graph.requires:`.
    6. **No** tocar el body (`# Phase ...`) excepto para los fixes de drift en los puntos 7–8 abajo.

    **Drift fixes (solo en 2 archivos):**

    7. `.planning/milestones/v1.0-phases/01-backend-core/01-02-SUMMARY.md`:
       - Reemplazar el string exacto `5 achievement definitions` por `12 achievement definitions` (línea 44 según conteo actual).

    8. `.planning/milestones/v1.0-phases/02-integraci-n-y-api/02-02-SUMMARY.md`:
       - Reemplazar `returns all 5 achievements with tier/progress` por `returns all 12 achievements with tier/progress`.
       - Reemplazar `returns all 5 achievement definitions with holders` por `returns all 12 achievement definitions with holders`.

    **Reglas globales:**
    - Idempotencia: el plan se debe poder re-ejecutar sin efecto adicional.
    - No reordenar el resto del frontmatter.
    - Mantener exactly el mismo estilo YAML que el archivo ya usa (algunas SUMMARYs usan listas inline `[a, b]`, otras multilinea con `-`; preferir el estilo dominante del archivo, default a inline).
    - No agregar `requirements: []` vacío — si un plan no tiene IDs (ninguno en este conjunto), saltearlo.
  </action>
  <verify>
    <automated>for s in .planning/milestones/v1.0-phases/*/*-SUMMARY.md; do FM=$(awk '/^---$/{n++; next} n==1' "$s"); echo "$FM" | grep -q "^requirements:" || { echo "MISSING requirements in $s"; exit 1; }; echo "$FM" | grep -q "^requirements-completed:" || { echo "MISSING requirements-completed in $s"; exit 1; }; done && ! grep -q "5 achievement definitions" .planning/milestones/v1.0-phases/01-backend-core/01-02-SUMMARY.md && ! grep -q "all 5 achievement" .planning/milestones/v1.0-phases/02-integraci-n-y-api/02-02-SUMMARY.md && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - Cada una de las 8 SUMMARYs tiene `requirements:` y `requirements-completed:` en posición top-level del frontmatter.
    - Las listas de REQs por plan coinciden con el mapeo del `<objective>`.
    - `01-02-SUMMARY.md` ya no contiene la subcadena `5 achievement definitions`.
    - `02-02-SUMMARY.md` ya no contiene las subcadenas `all 5 achievements` ni `all 5 achievement definitions`.
    - `git diff --stat` lista exactamente los 8 archivos en `files_modified` (más SUMMARY.md cuando se cree).
    - Diff de cada SUMMARY no toca dependencias, decisions, deviations, ni body fuera de las 3 líneas con drift.
  </acceptance_criteria>
  <done>
    Las 8 SUMMARYs v1.0 son escaneables por `grep "^requirements:" -A1` para extraer trazabilidad sin abrir VERIFICATION.md. Drift de conteo cerrado.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-01 | I (Information Disclosure) | docs only | accept | Edits son a archivos de planning. No hay PII, no hay surface change. |
</threat_model>

<verification>
1. Las 8 SUMMARYs tienen `requirements:` top-level — `for s in .planning/milestones/v1.0-phases/*/*-SUMMARY.md; do awk '/^---$/{n++; next} n==1' "$s" | grep -q "^requirements:"; done`.
2. Las 8 SUMMARYs tienen `requirements-completed:` top-level — análogo.
3. `! grep -q "5 achievement definitions" .planning/milestones/v1.0-phases/01-backend-core/01-02-SUMMARY.md`
4. `! grep -q "all 5 achievement" .planning/milestones/v1.0-phases/02-integraci-n-y-api/02-02-SUMMARY.md`
5. `git diff --stat` solo lista los 8 SUMMARYs (más el SUMMARY del plan 07-01 al final).
</verification>

<success_criteria>
1. Roadmap success criterion #1 (Phase 01 SUMMARY refleja 12 evaluadores) — cubierto por el fix en 01-02-SUMMARY.md.
2. Roadmap success criterion #2 (las 8 SUMMARYs v1.0 con frontmatter estandarizado) — cubierto.
3. Cierra los items cross-cutting "DOC DRIFT: 5 evaluators" y "Process: SUMMARY frontmatter inconsistent" del audit.
</success_criteria>

<output>
Tras completar el plan, crear `.planning/phases/07-documentacion-y-proceso/07-01-SUMMARY.md` siguiendo `$HOME/.claude/get-shit-done/templates/summary.md`. Incluir:
- Frontmatter estándar con `requirements: []` y `requirements-completed: []` (este plan no implementa REQs nuevos — es proceso/docs).
- Lista de los 8 archivos modificados.
- Mapeo plan→REQ aplicado.
</output>
