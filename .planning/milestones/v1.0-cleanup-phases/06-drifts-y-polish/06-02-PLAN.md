---
phase: 06-drifts-y-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx
  - frontend/src/components/AchievementCard/AchievementCard.tsx
  - frontend/src/components/AchievementModal/AchievementModal.tsx
  - frontend/src/pages/PlayerProfile/PlayerProfile.tsx
  - frontend/src/test/components/AchievementBadgeMini.test.tsx
  - frontend/src/test/components/AchievementCard.test.tsx
autonomous: true
requirements:
  - ENDG-02
  - ENDG-03
  - PROF-02
gap_closure:
  - drift-AchievementBadgeMini-is_upgrade-unused-prop
  - drift-AchievementCard-max_tier-unused-prop
threat_model_severity: low
must_haves:
  truths:
    - "AchievementBadgeMini ya no declara is_upgrade en su interfaz de props"
    - "AchievementCard ya no declara max_tier en su interfaz de props"
    - "Los callers (AchievementModal y PlayerProfile) ya no pasan los props eliminados"
    - "El comportamiento visual de ambos componentes queda idéntico (data-type='new'|'upgrade' sigue derivándose de is_new; ProgressBar y counter siguen mostrándose igual)"
    - "El typecheck y la suite vitest del frontend siguen pasando, con tests reformulados donde correspondía"
  artifacts:
    - path: "frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx"
      provides: "Componente con interfaz limpia: title, tier, fallback_icon, is_new"
      contains: "interface AchievementBadgeMiniProps"
      not_contains: "is_upgrade"
    - path: "frontend/src/components/AchievementCard/AchievementCard.tsx"
      provides: "Componente con interfaz limpia, sin max_tier"
      contains: "interface AchievementCardProps"
      not_contains: "max_tier"
    - path: "frontend/src/components/AchievementModal/AchievementModal.tsx"
      provides: "Caller que ya no pasa is_upgrade a AchievementBadgeMini"
      contains: "AchievementBadgeMini"
      not_contains: "is_upgrade={"
    - path: "frontend/src/pages/PlayerProfile/PlayerProfile.tsx"
      provides: "Página que ya no pasa max_tier a AchievementCard"
      contains: "AchievementCard"
      not_contains: "max_tier={"
    - path: "frontend/src/test/components/AchievementBadgeMini.test.tsx"
      provides: "Tests adaptados al cambio: el caso `data-type='upgrade'` se reformula a partir de is_new=false; baseProps sin is_upgrade"
      contains: "describe('AchievementBadgeMini'"
    - path: "frontend/src/test/components/AchievementCard.test.tsx"
      provides: "Tests adaptados: baseProps sin max_tier"
      contains: "describe('AchievementCard'"
  key_links:
    - from: "frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx"
      to: "data-type attribute"
      via: "data-type derivado de is_new (`is_new ? 'new' : 'upgrade'`)"
      pattern: "is_new \\? 'new' : 'upgrade'"
    - from: "frontend/src/components/AchievementModal/AchievementModal.tsx"
      to: "AchievementBadgeMini"
      via: "JSX <AchievementBadgeMini ... is_new={ach.is_new} /> sin is_upgrade"
      pattern: "<AchievementBadgeMini"
    - from: "frontend/src/pages/PlayerProfile/PlayerProfile.tsx"
      to: "AchievementCard"
      via: "JSX <AchievementCard title=... description=... tier=... unlocked=... progress=... /> sin max_tier"
      pattern: "<AchievementCard"
---

<objective>
Eliminar dos props muertos detectados en el audit `v1.0-MILESTONE-AUDIT.md` §4 (drifts de severidad **low**, Phase 03):

1. **`AchievementBadgeMini.is_upgrade`** — declarado en la interfaz (línea 9) y pasado por `AchievementModal` (línea 29), pero **nunca leído** dentro del componente. La diferenciación visual `data-type="new"|"upgrade"` se deriva exclusivamente de `is_new` (línea 18: `is_new ? 'new' : 'upgrade'`), por lo que `is_upgrade` es estructuralmente redundante.
2. **`AchievementCard.max_tier`** — declarado en la interfaz (línea 10) y pasado por `PlayerProfile.tsx` (línea 165), pero **no destructurado** en el cuerpo del componente. El "indicador de tier máximo" descripto en `PROF-02` no está visible en runtime (silent absence).

**Por qué removemos en lugar de implementar el max-tier:** El audit lo cataloga como drift, no como feature missing. `PROF-02` se reportó como satisfecho en VERIFICATION (35/35), y agregar el indicador ahora cae fuera del scope cosmético de la Phase 6 (sería contenido nuevo de UI, no cleanup). Si el negocio lo quisiera de vuelta, debería entrar como item del backlog v2 con su propio plan, no colarse acá.

**Output:** dos componentes con interfaces minimales, dos callers limpios y dos suites de tests reformuladas (la de Badge necesita además que el test "data-type='upgrade'" deje de pasar `is_upgrade` y se exprese como `is_new={false}`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/milestones/v1.0-REQUIREMENTS.md
@./CLAUDE.md

<interfaces>
<!-- Estado actual de los componentes y callers afectados. Las líneas se eliminan/limpian sin renombrar nada. -->

frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx (estado actual):
```tsx
interface AchievementBadgeMiniProps {
  title: string
  tier: number
  fallback_icon: string
  is_new: boolean
  is_upgrade: boolean    // ← SE ELIMINA (declarado, nunca leído)
}

export default function AchievementBadgeMini({
  title,
  tier,
  fallback_icon,
  is_new,
  // is_upgrade NO está destructurado — confirma que era prop muerto
}: AchievementBadgeMiniProps) {
  const badgeType = is_new ? 'new' : 'upgrade'
  const badgeVariantClass = is_new ? styles.badgeNew : styles.badgeUpgrade
  const tierPillClass = is_new ? styles.tierPillNew : styles.tierPillUpgrade
  ...
}
```

frontend/src/components/AchievementModal/AchievementModal.tsx — caller a limpiar (líneas 22-30):
```tsx
{achList.map(ach => (
  <AchievementBadgeMini
    key={ach.code}
    title={ach.title}
    tier={ach.tier}
    fallback_icon={ach.fallback_icon}
    is_new={ach.is_new}
    is_upgrade={ach.is_upgrade}    // ← SE ELIMINA
  />
))}
```
Notas:
- `ach.is_upgrade` viene de `AchievementUnlockedDTO.is_upgrade` (backend `schemas/achievement.py:11`) y es legítimo en el DTO. **No tocar el DTO ni el tipo TypeScript**: el campo lo sigue calculando el backend (`evaluation_result_to_unlocked_dto` línea 30) y otros consumidores podrían leerlo en el futuro. Solo se elimina su uso como prop del componente.

frontend/src/components/AchievementCard/AchievementCard.tsx (estado actual):
```tsx
interface AchievementCardProps {
  title: string
  description: string
  fallback_icon: string
  tier: number
  max_tier: number     // ← SE ELIMINA (declarado, nunca destructurado)
  unlocked: boolean
  progress: { current: number; target: number } | null
  onClick?: () => void
}

export default function AchievementCard({
  title,
  description,
  fallback_icon,
  tier,
  unlocked,
  progress,
  onClick,
  // max_tier NO está destructurado — confirma que era prop muerto
}: AchievementCardProps) {
  ...
}
```

frontend/src/pages/PlayerProfile/PlayerProfile.tsx — caller a limpiar (líneas 158-168):
```tsx
{sortedAchievements.map(ach => (
  <AchievementCard
    key={ach.code}
    title={ach.title}
    description={ach.description}
    fallback_icon={ach.fallback_icon}
    tier={ach.tier}
    max_tier={ach.max_tier}    // ← SE ELIMINA
    unlocked={ach.unlocked}
    progress={ach.progress}
  />
))}
```
Nota:
- `ach.max_tier` proviene de `PlayerAchievementDTO.max_tier` (backend `schemas/achievement.py:30` + frontend `types/index.ts:175`). **No se toca**: lo sigue calculando el mapper (`build_player_achievement_dto` línea 47) y un futuro indicador podría re-consumirlo. Solo se deja de pasarlo a este componente concreto.

frontend/src/test/components/AchievementBadgeMini.test.tsx — fixture y caso a reformular:

`baseProps` actual (líneas 5-11):
```tsx
const baseProps = {
  title: 'Gran Terraformador',
  tier: 3,
  fallback_icon: 'trophy',
  is_new: true,
  is_upgrade: false,    // ← SE ELIMINA del fixture
}
```

Test cuestionado (líneas 40-46):
```tsx
it('uses data-type="upgrade" when is_upgrade=true', () => {
  const { container } = render(
    <AchievementBadgeMini {...baseProps} is_new={false} is_upgrade={true} />,
  )
  const badge = container.firstChild as HTMLElement
  expect(badge.getAttribute('data-type')).toBe('upgrade')
})
```
Reformulación correcta (la verdad de fondo es que `data-type` deriva de `is_new`, no de `is_upgrade`):
```tsx
it('uses data-type="upgrade" when is_new=false', () => {
  const { container } = render(
    <AchievementBadgeMini {...baseProps} is_new={false} />,
  )
  const badge = container.firstChild as HTMLElement
  expect(badge.getAttribute('data-type')).toBe('upgrade')
})
```

frontend/src/test/components/AchievementCard.test.tsx — fixture a limpiar (líneas 5-13):
```tsx
const baseProps = {
  title: 'Test Achievement',
  description: 'Test description',
  fallback_icon: 'trophy',
  tier: 2,
  max_tier: 5,    // ← SE ELIMINA del fixture
  unlocked: true,
  progress: null,
}
```
No hay assertions que dependan de `max_tier` (la suite no espera ningún render de "max tier"). Solo se quita del fixture.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Limpiar props muertos en AchievementBadgeMini + AchievementCard</name>
  <files>frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx, frontend/src/components/AchievementCard/AchievementCard.tsx</files>
  <read_first>
    - frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx (todo el archivo — confirmar que `is_upgrade` solo aparece en la interfaz, no en la destructuración ni el cuerpo)
    - frontend/src/components/AchievementCard/AchievementCard.tsx (todo el archivo — confirmar que `max_tier` solo aparece en la interfaz, no en la destructuración ni el cuerpo)
    - Antes de editar, smoke search de cualquier uso oculto:
      `grep -n "is_upgrade" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx`
      `grep -n "max_tier" frontend/src/components/AchievementCard/AchievementCard.tsx`
      Resultado esperado en ambos: una única coincidencia, dentro del bloque `interface ...Props`. Si aparece más de una, detener y reportar.
  </read_first>
  <action>
    **A) Editar `frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx`:**

    1. En la interfaz `AchievementBadgeMiniProps`, eliminar la línea `  is_upgrade: boolean`.
    2. **NO** modificar la firma de la destructuración del componente (ya no incluía `is_upgrade`).
    3. **NO** modificar el cuerpo (`badgeType`, `badgeVariantClass`, `tierPillClass`, JSX). La derivación `data-type` desde `is_new` queda intacta — esa es la regla de comportamiento que el componente preserva.
    4. **NO** tocar el archivo `AchievementBadgeMini.module.css`.

    **B) Editar `frontend/src/components/AchievementCard/AchievementCard.tsx`:**

    1. En la interfaz `AchievementCardProps`, eliminar la línea `  max_tier: number`.
    2. **NO** modificar la firma de la destructuración (ya no incluía `max_tier`).
    3. **NO** modificar el cuerpo (`progressPercentage`, `counter`, `cardClasses`, JSX). El render de `NIVEL {unlocked ? tier : 0}` se queda como está.
    4. **NO** tocar el archivo `AchievementCard.module.css`.

    **C) Smoke check post-edición (debe ejecutarse antes de pasar a Tarea 2):**

    ```bash
    grep -n "is_upgrade" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx
    grep -n "max_tier" frontend/src/components/AchievementCard/AchievementCard.tsx
    ```
    Resultado esperado en ambos: 0 coincidencias.

    **D) NO hacer:**
    - No tocar la interfaz `AchievementUnlockedDTO` ni `PlayerAchievementDTO` en `types/index.ts` ni el schema backend. Esos campos siguen siendo legítimos en el DTO.
    - No reformatear el resto del componente (sin reordenamiento de imports, sin cambios de styling).
    - No agregar comentarios "removed unused prop" — el SUMMARY del plan documenta el cambio.
  </action>
  <verify>
    <automated>! grep -n "is_upgrade" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx &amp;&amp; ! grep -n "max_tier" frontend/src/components/AchievementCard/AchievementCard.tsx &amp;&amp; grep -q "interface AchievementBadgeMiniProps" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx &amp;&amp; grep -q "interface AchievementCardProps" frontend/src/components/AchievementCard/AchievementCard.tsx &amp;&amp; grep -q "is_new \? 'new' : 'upgrade'" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "is_upgrade" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx` retorna 0 (cero ocurrencias).
    - `! grep -n "max_tier" frontend/src/components/AchievementCard/AchievementCard.tsx` retorna 0 (cero ocurrencias).
    - La interfaz `AchievementBadgeMiniProps` sigue presente y declara exactamente: `title: string`, `tier: number`, `fallback_icon: string`, `is_new: boolean`.
    - La interfaz `AchievementCardProps` sigue presente y declara: `title`, `description`, `fallback_icon`, `tier`, `unlocked`, `progress`, `onClick?`.
    - La línea `const badgeType = is_new ? 'new' : 'upgrade'` (o equivalente que derive `data-type` desde `is_new`) sigue intacta.
    - `git diff` solo lista los dos componentes para esta tarea (los callers y tests son responsabilidad de Tarea 2 y 3).
  </acceptance_criteria>
  <done>
    Las dos interfaces ya no declaran props muertos; el comportamiento del render no cambió.
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Limpiar callers — AchievementModal y PlayerProfile dejan de pasar los props eliminados</name>
  <files>frontend/src/components/AchievementModal/AchievementModal.tsx, frontend/src/pages/PlayerProfile/PlayerProfile.tsx</files>
  <read_first>
    - frontend/src/components/AchievementModal/AchievementModal.tsx (todo el archivo — confirmar que es el único caller de AchievementBadgeMini en la app: `grep -rn "AchievementBadgeMini" frontend/src` debe listar solo este archivo y el test del propio componente)
    - frontend/src/pages/PlayerProfile/PlayerProfile.tsx (todo el archivo — confirmar que es el único caller de AchievementCard en la app: `grep -rn "AchievementCard" frontend/src` debe listar solo este archivo, el componente y su test)
    - Resultado de Tarea 1 (las interfaces ya no declaran los props — pasar `is_upgrade={...}` o `max_tier={...}` ahora produciría error de typecheck)
  </read_first>
  <action>
    **A) Editar `frontend/src/components/AchievementModal/AchievementModal.tsx`:**

    En el JSX `<AchievementBadgeMini ... />` (líneas 22-30), eliminar exclusivamente la línea:
    ```tsx
    is_upgrade={ach.is_upgrade}
    ```

    El bloque resultante:
    ```tsx
    <AchievementBadgeMini
      key={ach.code}
      title={ach.title}
      tier={ach.tier}
      fallback_icon={ach.fallback_icon}
      is_new={ach.is_new}
    />
    ```

    NO tocar:
    - El destructuring de `playerEntries`.
    - El render del nombre del jugador.
    - El botón "Continuar".
    - El import de `AchievementBadgeMini`.

    **B) Editar `frontend/src/pages/PlayerProfile/PlayerProfile.tsx`:**

    En el JSX `<AchievementCard ... />` (líneas 158-168), eliminar exclusivamente la línea:
    ```tsx
    max_tier={ach.max_tier}
    ```

    El bloque resultante:
    ```tsx
    <AchievementCard
      key={ach.code}
      title={ach.title}
      description={ach.description}
      fallback_icon={ach.fallback_icon}
      tier={ach.tier}
      unlocked={ach.unlocked}
      progress={ach.progress}
    />
    ```

    NO tocar:
    - `useEffect`, `Promise.all`, `getPlayerProfile`, `getPlayers`, `getPlayerAchievements`.
    - Las tabs (`activeTab`, `handleTabChange`, etc.).
    - El render de stats, records, ni el resto de la página.
    - El sorted/unsorted de `sortedAchievements` (sigue ordenando unlocked-first; nada que ver con `max_tier`).

    **C) Smoke check post-edición:**
    ```bash
    grep -n "is_upgrade={" frontend/src/components/AchievementModal/AchievementModal.tsx
    grep -n "max_tier={" frontend/src/pages/PlayerProfile/PlayerProfile.tsx
    grep -rn "is_upgrade=" frontend/src/
    grep -rn "max_tier=" frontend/src/
    ```
    Resultado esperado:
    - Las dos primeras grep: 0 coincidencias.
    - Las dos siguientes (búsquedas globales): solo coincidencias dentro de los **tests** que la Tarea 3 corregirá. Si aparece **fuera** de los tests, hay un caller olvidado y debe limpiarse en este mismo paso.

    **D) NO hacer:**
    - No tocar ningún DTO. `ach.is_upgrade` y `ach.max_tier` siguen vivos en los tipos backend/frontend (son datos del response, no contrato de UI).
    - No mover el orden de imports.
    - No introducir comentarios.
  </action>
  <verify>
    <automated>! grep -n "is_upgrade=" frontend/src/components/AchievementModal/AchievementModal.tsx &amp;&amp; ! grep -n "max_tier=" frontend/src/pages/PlayerProfile/PlayerProfile.tsx &amp;&amp; grep -q "<AchievementBadgeMini" frontend/src/components/AchievementModal/AchievementModal.tsx &amp;&amp; grep -q "<AchievementCard" frontend/src/pages/PlayerProfile/PlayerProfile.tsx &amp;&amp; grep -q "is_new={ach.is_new}" frontend/src/components/AchievementModal/AchievementModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "is_upgrade=" frontend/src/components/AchievementModal/AchievementModal.tsx` retorna 0.
    - `! grep -n "max_tier=" frontend/src/pages/PlayerProfile/PlayerProfile.tsx` retorna 0.
    - `<AchievementBadgeMini` y `<AchievementCard` siguen presentes en los respectivos archivos (no se borraron por accidente).
    - El JSX resultante mantiene `is_new={ach.is_new}` (caller del Modal) y `progress={ach.progress}` (caller del Profile) — los props que sí leen los componentes.
    - `grep -rn "is_upgrade=" frontend/src/ | grep -v "frontend/src/test/"` no devuelve nada (fuera de tests, ya no se pasa el prop).
    - `grep -rn "max_tier=" frontend/src/ | grep -v "frontend/src/test/"` no devuelve nada.
  </acceptance_criteria>
  <done>
    Los dos callers exclusivos de los componentes afectados ya no pasan los props eliminados. Los tipos del DTO permanecen intactos.
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Adaptar tests vitest (fixtures + caso reformulado en BadgeMini) y validar typecheck + suite</name>
  <files>frontend/src/test/components/AchievementBadgeMini.test.tsx, frontend/src/test/components/AchievementCard.test.tsx</files>
  <read_first>
    - frontend/src/test/components/AchievementBadgeMini.test.tsx (todo el archivo)
    - frontend/src/test/components/AchievementCard.test.tsx (todo el archivo)
    - Resultado de Tareas 1 y 2 (las interfaces ya no aceptan `is_upgrade`/`max_tier`; pasar esos props en tests genera typecheck errors)
    - Antes de editar, descubrir cualquier otro test que importe estos componentes:
      `grep -rn "AchievementBadgeMini\|AchievementCard" frontend/src/test/`
      Si hay otros tests/integraciones que pasen estos props, también deben adaptarse en este paso.
  </read_first>
  <action>
    **A) Editar `frontend/src/test/components/AchievementBadgeMini.test.tsx`:**

    1. En el `baseProps` (líneas 5-11), eliminar la línea `  is_upgrade: false,`. El fixture queda con: `title`, `tier`, `fallback_icon`, `is_new`.
    2. **Test "uses data-type='upgrade' when is_upgrade=true" (líneas 40-46) — REFORMULAR:**

       Cambiar el nombre del caso a `uses data-type="upgrade" when is_new=false` y eliminar el override `is_upgrade={true}` del JSX (queda solo `is_new={false}`):

       Versión nueva:
       ```tsx
       it('uses data-type="upgrade" when is_new=false', () => {
         const { container } = render(
           <AchievementBadgeMini {...baseProps} is_new={false} />,
         )
         const badge = container.firstChild as HTMLElement
         expect(badge.getAttribute('data-type')).toBe('upgrade')
       })
       ```

       **Justificación (sin agregar al código del test):** la verdad de fondo del componente es que `data-type` se deriva exclusivamente de `is_new`. El test anterior pasaba por accidente porque el fixture seteaba `is_new=false` mientras se enfatizaba `is_upgrade=true`. Reformular alinea el test con la regla real.

    3. **Test "uses data-type='new' when is_new=true" (líneas 32-38) — limpiar override:**

       Versión actual incluye `is_upgrade={false}` como override. Eliminarlo:
       ```tsx
       it('uses data-type="new" when is_new=true', () => {
         const { container } = render(
           <AchievementBadgeMini {...baseProps} is_new={true} />,
         )
         const badge = container.firstChild as HTMLElement
         expect(badge.getAttribute('data-type')).toBe('new')
       })
       ```

    4. NO tocar los otros 3 casos (renders title, renders SVG size 20, "NIVEL 3" pill) — siguen siendo válidos sin cambios.

    **B) Editar `frontend/src/test/components/AchievementCard.test.tsx`:**

    1. En el `baseProps` (líneas 5-13), eliminar la línea `  max_tier: 5,`. El fixture queda con: `title`, `description`, `fallback_icon`, `tier`, `unlocked`, `progress`.
    2. NO tocar ningún test (ninguno asertaba algo sobre `max_tier`).

    **C) Ejecutar la verificación completa:**

    ```bash
    cd frontend && npm run typecheck && npm test -- --run --reporter=basic
    ```

    **Si typecheck falla:**
    - Si el error es `Property 'is_upgrade' does not exist on type 'AchievementBadgeMiniProps'` o `Property 'max_tier' does not exist on type 'AchievementCardProps'` en algún archivo no listado en este plan → ese archivo es un consumidor olvidado; limpiarlo (eliminar el prop) en este mismo paso. Documentar en el SUMMARY.
    - Re-ejecutar.

    **Si vitest falla:**
    - Si falla porque el caso reformulado todavía pasa `is_upgrade={true}`, completar la edición.
    - Si falla por una assertion sobre `data-type='upgrade'` que no encuentra ese atributo, revisar que `AchievementBadgeMini.tsx` no haya sido tocado por error (el `data-type` debe seguir derivándose de `is_new`).
    - **No reactivar** los props eliminados para "satisfacer" el test.
    - Re-ejecutar hasta exit 0.

    **D) NO hacer:**
    - No correr `npm test` sin `-- --run` (queda en watch mode).
    - No editar `setup.ts`, `vitest.config.ts`, ni mocks globales.
    - No inventar casos nuevos para "compensar" la reformulación. El espacio de comportamiento que se prueba es exactamente el mismo (data-type new vs upgrade).
  </action>
  <verify>
    <automated>! grep -n "is_upgrade" frontend/src/test/components/AchievementBadgeMini.test.tsx &amp;&amp; ! grep -n "max_tier" frontend/src/test/components/AchievementCard.test.tsx &amp;&amp; grep -q "data-type=\"upgrade\" when is_new=false" frontend/src/test/components/AchievementBadgeMini.test.tsx &amp;&amp; cd frontend &amp;&amp; npm run typecheck &amp;&amp; npm test -- --run --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "is_upgrade" frontend/src/test/components/AchievementBadgeMini.test.tsx` retorna 0 (cero coincidencias en el archivo de test).
    - `! grep -n "max_tier" frontend/src/test/components/AchievementCard.test.tsx` retorna 0.
    - El test reformulado existe: `grep -q 'data-type="upgrade" when is_new=false' frontend/src/test/components/AchievementBadgeMini.test.tsx` (texto exacto del nombre del caso).
    - `cd frontend && npm run typecheck` exit code 0.
    - `cd frontend && npm test -- --run` exit code 0; los 5 casos de BadgeMini siguen presentes (renombrado uno) y los 10 casos de Card siguen presentes (solo se tocó el fixture).
    - Búsqueda global de cualquier prop residual: `grep -rn "is_upgrade=\|max_tier=" frontend/src/` no devuelve coincidencias en components/ ni pages/ ni test/. (Solo es aceptable si aparece dentro del DTO definition en `types/index.ts`, donde sigue legítimamente: `is_upgrade: boolean` y `max_tier: number` son **declaraciones de propiedad**, no llamadas con `=`; el patrón `=` en grep las descarta naturalmente.)
  </acceptance_criteria>
  <done>
    Tests reformulados y verdes. La regla de fondo (data-type derivado de is_new) ahora está expresada explícitamente en el nombre del caso. Fixture de Card sin `max_tier`. Typecheck verde.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Modal/Profile) → componente | Reducción de la firma de props. Los datos eliminados (`is_upgrade`, `max_tier`) siguen disponibles en el DTO; solo dejan de cruzar este límite. |
| componente → render DOM | Sin cambios: `data-type`, `NIVEL`, `ProgressBar` y `counter` se computan igual que antes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T (Tampering) | AchievementBadgeMini.tsx | accept | El componente nunca leía `is_upgrade`. Eliminar el prop no altera lógica ni introduce nueva superficie. La derivación de `data-type` sigue dependiendo solo de `is_new`. |
| T-06-05 | I (Information Disclosure) | AchievementCard.tsx | accept | `max_tier` no se renderizaba; eliminarlo del prop no oculta información que el usuario veía. La info sigue disponible en el DTO si una vista futura la necesita. |
| T-06-06 | E (Elevation) | tests | accept | Los tests reformulados expresan la misma regla de comportamiento (data-type por is_new). No se reduce cobertura ni se elimina assertion. |
</threat_model>

<verification>
**Comprobaciones automáticas (deben pasar todas):**

1. `! grep -n "is_upgrade" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx`
2. `! grep -n "max_tier" frontend/src/components/AchievementCard/AchievementCard.tsx`
3. `! grep -n "is_upgrade=" frontend/src/components/AchievementModal/AchievementModal.tsx`
4. `! grep -n "max_tier=" frontend/src/pages/PlayerProfile/PlayerProfile.tsx`
5. `! grep -n "is_upgrade" frontend/src/test/components/AchievementBadgeMini.test.tsx`
6. `! grep -n "max_tier" frontend/src/test/components/AchievementCard.test.tsx`
7. `grep -q 'data-type="upgrade" when is_new=false' frontend/src/test/components/AchievementBadgeMini.test.tsx`
8. `grep -q "is_new \\? 'new' : 'upgrade'" frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx` (regla de fondo intacta)
9. `! grep -rn "is_upgrade=\|max_tier=" frontend/src/` (búsqueda global; cero coincidencias).
10. `cd frontend && npm run typecheck` exit code 0.
11. `cd frontend && npm test -- --run` exit code 0.
</verification>

<success_criteria>
1. `AchievementBadgeMini` y `AchievementCard` declaran solo los props que efectivamente consumen (cierra Success Criterion #2 de ROADMAP — parte 1).
2. Los componentes padre (`AchievementModal`, `PlayerProfile`) dejan de pasar los props eliminados (cierra Success Criterion #2 de ROADMAP — parte 2).
3. Los tests vitest siguen verdes; el caso "data-type='upgrade'" queda reformulado para reflejar la regla real (deriva de `is_new`, no de `is_upgrade`).
4. Cierra los items de tech_debt §8 Phase 03 del audit: `AchievementBadgeMini.is_upgrade` unused prop y `AchievementCard.max_tier` unused prop.
5. Cubre los requirements **ENDG-02** (badge mini con ícono + título), **ENDG-03** (diferenciación visual nuevo vs mejorado, ahora confirmada por la única señal real: `is_new`) y **PROF-02** (card sin prop muerto que sugiriera un indicador de tier máximo no implementado).
</success_criteria>

<output>
Tras completar el plan, crear `.planning/phases/06-drifts-y-polish/06-02-SUMMARY.md` siguiendo `$HOME/.claude/get-shit-done/templates/summary.md`. Incluir en el frontmatter:
- `requirements: [ENDG-02, ENDG-03, PROF-02]`
- `requirements-completed: [ENDG-02, ENDG-03, PROF-02]`
- `gap_closure: [drift-AchievementBadgeMini-is_upgrade-unused-prop, drift-AchievementCard-max_tier-unused-prop]`
- Diff de los 6 archivos modificados (2 componentes, 2 callers, 2 tests).
- Salida resumida de `npm run typecheck` y `npm test -- --run` confirmando verde.
- Nota explícita: el test "data-type='upgrade'" fue reformulado a "when is_new=false" para alinear con la regla real del componente; los DTOs (`AchievementUnlockedDTO.is_upgrade`, `PlayerAchievementDTO.max_tier`) siguen intactos por diseño.
</output>
</content>
</invoke>