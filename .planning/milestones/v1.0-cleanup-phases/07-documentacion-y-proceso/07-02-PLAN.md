---
phase: 07-documentacion-y-proceso
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
gap_closure:
  - PROCESS-requirements-traceability-not-auto-updated
threat_model_severity: low
must_haves:
  truths:
    - "El archivo `.planning/milestones/v1.0-REQUIREMENTS.md` tiene 35/35 requirements en estado `Complete` en su tabla Traceability"
    - "Cero items con status `Pending` o sin status en la tabla Traceability"
    - "El SUMMARY de este plan documenta la verificación con un grep reproducible y un commit hash de evidencia"
  artifacts:
    - path: ".planning/milestones/v1.0-REQUIREMENTS.md"
      provides: "Tabla de Traceability con 35/35 requirements en estado Complete"
      contains: "Mapped to phases: 35"
---

<objective>
Cerrar el item cross-cutting "REQUIREMENTS.md traceability not auto-updated when phases completed" del audit `v1.0-MILESTONE-AUDIT.md`.

**Por qué:** El audit lo lista como tech_debt; el RETROSPECTIVE.md también lo señala ("3 requirements showed Pending despite being implemented"). El issue puede haberse resuelto manualmente con commits posteriores. Este plan confirma el estado actual con evidencia y, si quedan inconsistencias, las repara.

**Output esperado:** verificación read-only documentada en SUMMARY (sin edits si todo está OK), o un commit puntual si quedan items pendientes.
</objective>

<tasks>

<task type="auto">
  <name>Tarea 1: Auditar la tabla Traceability de v1.0-REQUIREMENTS.md</name>
  <files>(read-only sobre `.planning/milestones/v1.0-REQUIREMENTS.md`)</files>
  <read_first>
    - `.planning/milestones/v1.0-REQUIREMENTS.md` (sección `## Traceability`)
    - `.planning/v1.0-MILESTONE-AUDIT.md` (cross-cutting tech_debt)
    - `.planning/RETROSPECTIVE.md` (mención al issue)
  </read_first>
  <action>
    Ejecutar 3 chequeos:

    1. **Conteo de requirements totales declarados** en el cuerpo del archivo (líneas que empiezan con `- [x]` o `- [ ]` debajo de los `### Backend Core`, `### Persistencia`, `### Integración`, `### API`, `### Frontend — Fin de Partida`, `### Frontend — Perfil`, `### Frontend — Catálogo`, `### Íconos`, `### Herramientas`):
       ```bash
       grep -cE "^- \[[x ]\] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md
       ```
       **Esperado:** 35.

    2. **Conteo de items chequeados (`[x]`) vs no chequeados (`[ ]`)**:
       ```bash
       grep -cE "^- \[x\] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md
       grep -cE "^- \[ \] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md
       ```
       **Esperado:** checked=35, unchecked=0.

    3. **Tabla Traceability**: contar filas con `Complete` vs sin `Complete`:
       ```bash
       sed -n '/^## Traceability/,$p' .planning/milestones/v1.0-REQUIREMENTS.md | grep -cE "\| Complete"
       sed -n '/^## Traceability/,$p' .planning/milestones/v1.0-REQUIREMENTS.md | grep -cE "\| Pending"
       ```
       **Esperado:** Complete=35 (incluye `Complete (adjusted ...)` para ICON-02), Pending=0.
  </action>
  <verify>
    <automated>TOTAL=$(grep -cE "^- \[[x ]\] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md); CHECKED=$(grep -cE "^- \[x\] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md); UNCHECKED=$(grep -cE "^- \[ \] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md); PENDING=$(sed -n '/^## Traceability/,$p' .planning/milestones/v1.0-REQUIREMENTS.md | grep -cE "\| Pending"); test "$TOTAL" -eq 35 && test "$CHECKED" -eq 35 && test "$UNCHECKED" -eq 0 && test "$PENDING" -eq 0 && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - `TOTAL=35`, `CHECKED=35`, `UNCHECKED=0`, `PENDING=0` en `v1.0-REQUIREMENTS.md`.
    - Si falla: investigar la inconsistencia, parchearla con un commit puntual, re-correr el verify.
  </acceptance_criteria>
  <done>
    Tabla Traceability confirmada como completa (35/35). El item de tech_debt queda cerrado con evidencia.
  </done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-07-02 | I (Information Disclosure) | docs only | accept | Verificación read-only sobre archivo de planning. |
</threat_model>

<verification>
1. `grep -cE "^- \[ \]" .planning/milestones/v1.0-REQUIREMENTS.md` retorna 0 (cero items pending en cuerpo).
2. `sed -n '/^## Traceability/,$p' .planning/milestones/v1.0-REQUIREMENTS.md | grep -cE "\| Pending"` retorna 0.
3. `grep -cE "^- \[x\] \*\*[A-Z]+-[0-9]+\*\*" .planning/milestones/v1.0-REQUIREMENTS.md` retorna 35.
</verification>

<success_criteria>
1. Roadmap success criterion #3 (audit re-run shows zero `tech_debt` items in doc/process category) — combinado con plan 07-01 cierra los 3 items cross-cutting.
2. Cierra el item "REQUIREMENTS.md traceability not auto-updated" con evidencia documentada.
</success_criteria>

<output>
Tras completar el plan, crear `.planning/phases/07-documentacion-y-proceso/07-02-SUMMARY.md`. Incluir:
- Resultado de los 3 chequeos automáticos (números exactos).
- Confirmación de que no se requirieron edits (read-only verification).
</output>
