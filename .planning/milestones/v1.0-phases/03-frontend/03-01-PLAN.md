---
phase: 03-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/components/AchievementIcon/AchievementIcon.tsx
  - frontend/src/components/AchievementIcon/AchievementIcon.module.css
  - frontend/src/components/ProgressBar/ProgressBar.tsx
  - frontend/src/components/ProgressBar/ProgressBar.module.css
  - frontend/src/components/AchievementCard/AchievementCard.tsx
  - frontend/src/components/AchievementCard/AchievementCard.module.css
  - frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx
  - frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.module.css
  - frontend/src/components/TabBar/TabBar.tsx
  - frontend/src/components/TabBar/TabBar.module.css
  - frontend/src/test/components/AchievementIcon.test.tsx
  - frontend/src/test/components/ProgressBar.test.tsx
  - frontend/src/test/components/AchievementCard.test.tsx
  - frontend/src/test/components/AchievementBadgeMini.test.tsx
  - frontend/src/test/components/TabBar.test.tsx
autonomous: true
requirements: [ICON-01, ICON-03, ENDG-02, ENDG-03, PROF-02, PROF-03, PROF-04]

must_haves:
  truths:
    - "AchievementIcon renders a Lucide icon from a string key, with fallback to Trophy for unknown keys"
    - "AchievementIcon displays colored icon when unlocked and grayscale when locked"
    - "ProgressBar shows correct fill width as percentage of value prop"
    - "AchievementCard shows icon, title, description, NIVEL N, progress bar, and counter"
    - "AchievementCard locked state shows grayscale icon, NIVEL 0, empty progress bar"
    - "AchievementBadgeMini shows icon + title with accent border for new and success border for upgrade"
    - "TabBar renders 3 clickable tabs with active indicator"
  artifacts:
    - path: "frontend/src/components/AchievementIcon/AchievementIcon.tsx"
      provides: "Lucide icon mapping with fallback"
      exports: ["default"]
    - path: "frontend/src/components/ProgressBar/ProgressBar.tsx"
      provides: "Progress bar with 0-100 value"
      exports: ["default"]
    - path: "frontend/src/components/AchievementCard/AchievementCard.tsx"
      provides: "Full achievement card for profile and catalog"
      exports: ["default"]
    - path: "frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx"
      provides: "Mini badge for end-of-game modal"
      exports: ["default"]
    - path: "frontend/src/components/TabBar/TabBar.tsx"
      provides: "Horizontal tab bar"
      exports: ["default"]
  key_links:
    - from: "AchievementCard"
      to: "AchievementIcon"
      via: "import and render"
      pattern: "import AchievementIcon from.*AchievementIcon"
    - from: "AchievementCard"
      to: "ProgressBar"
      via: "import and render"
      pattern: "import ProgressBar from.*ProgressBar"
    - from: "AchievementBadgeMini"
      to: "AchievementIcon"
      via: "import and render"
      pattern: "import AchievementIcon from.*AchievementIcon"
---

<objective>
Build all leaf UI components for the achievements system: AchievementIcon, ProgressBar, AchievementCard, AchievementBadgeMini, and TabBar. Install lucide-react dependency.

Purpose: These are the building blocks consumed by plans 02 and 03. Building bottom-up avoids blocking and ensures each component is tested independently.
Output: 5 new components with CSS Modules and unit tests, lucide-react installed.
</objective>

<execution_context>
@/Users/facu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/facu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend/03-CONTEXT.md
@.planning/phases/03-frontend/03-UI-SPEC.md
@.planning/phases/03-frontend/03-RESEARCH.md

<interfaces>
<!-- Achievement DTOs from frontend/src/types/index.ts — use these types directly -->

```typescript
export interface AchievementUnlockedDTO {
  code: string
  title: string
  tier: number
  is_new: boolean
  is_upgrade: boolean
  icon: string | null
  fallback_icon: string
}

export interface ProgressDTO {
  current: number
  target: number
}

export interface PlayerAchievementDTO {
  code: string
  title: string
  description: string
  tier: number
  max_tier: number
  icon: string | null
  fallback_icon: string
  unlocked: boolean
  unlocked_at: string | null
  progress: ProgressDTO | null
}
```

<!-- Modal component from frontend/src/components/Modal/Modal.tsx -->
```typescript
interface ModalProps {
  title: string
  onClose: () => void
  children: ReactNode
}
export default function Modal({ title, onClose, children }: ModalProps)
```

<!-- Test pattern from frontend/src/test/components/Login.test.tsx -->
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { render, screen, fireEvent } from '@testing-library/react'
// renderComponent() helper wrapping providers as needed
// Query by role/label (accessibility-first)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install lucide-react + build AchievementIcon and ProgressBar</name>
  <files>
    frontend/package.json
    frontend/src/components/AchievementIcon/AchievementIcon.tsx
    frontend/src/components/AchievementIcon/AchievementIcon.module.css
    frontend/src/components/ProgressBar/ProgressBar.tsx
    frontend/src/components/ProgressBar/ProgressBar.module.css
    frontend/src/test/components/AchievementIcon.test.tsx
    frontend/src/test/components/ProgressBar.test.tsx
  </files>
  <read_first>
    frontend/package.json
    frontend/src/index.css
    frontend/src/components/Button/Button.tsx
    frontend/src/components/Button/Button.module.css
    frontend/src/test/components/Login.test.tsx
    frontend/src/types/index.ts
    .planning/phases/03-frontend/03-UI-SPEC.md
  </read_first>
  <behavior>
    AchievementIcon:
    - Renders Trophy icon when fallback_icon="trophy"
    - Renders Flame icon when fallback_icon="flame"
    - Renders Map icon when fallback_icon="map"
    - Renders Gamepad2 icon when fallback_icon="gamepad-2"
    - Renders Star icon when fallback_icon="star"
    - Renders Zap icon when fallback_icon="zap"
    - Falls back to Trophy for unknown fallback_icon values (e.g. "unknown")
    - Applies class "unlocked" when unlocked=true
    - Applies class "locked" when unlocked=false
    - Renders icon at default size 24
    - Renders icon at custom size when size prop provided

    ProgressBar:
    - Renders fill at 0% width when value=0
    - Renders fill at 50% width when value=50
    - Renders fill at 100% width when value=100
    - Clamps value to 0 when negative
    - Clamps value to 100 when over 100
    - Has role="progressbar" with aria-valuenow
  </behavior>
  <action>
    1. Run `cd frontend && npm install lucide-react` to add dependency.

    2. Create `frontend/src/components/AchievementIcon/AchievementIcon.tsx`:
       - Import named icons from lucide-react: `import { Trophy, Flame, Map, Gamepad2, Star, Zap } from 'lucide-react'`
       - Import `type { LucideProps } from 'lucide-react'`
       - Define `type IconComponent = React.FC<LucideProps>`
       - Static mapping `ICON_MAP: Record<string, IconComponent>` with keys: "trophy"->Trophy, "flame"->Flame, "map"->Map, "gamepad-2"->Gamepad2, "star"->Star, "zap"->Zap
       - Interface `AchievementIconProps { fallback_icon: string; size?: number; unlocked: boolean }`
       - Default export function receiving those props, defaulting size to 24
       - Resolve icon: `const Icon = ICON_MAP[fallback_icon] ?? Trophy`
       - Render: `<div className={[styles.circle, unlocked ? styles.unlocked : styles.locked].filter(Boolean).join(' ')}><Icon size={size} /></div>`

    3. Create `frontend/src/components/AchievementIcon/AchievementIcon.module.css`:
       - `.circle`: width 48px, height 48px, border-radius 50%, display flex, align-items center, justify-content center, background `var(--color-border)`, flex-shrink 0
       - `.unlocked`: color `var(--color-accent)`
       - `.locked`: color `var(--color-text-muted)`, filter grayscale(100%), opacity 0.5

    4. Create `frontend/src/components/ProgressBar/ProgressBar.tsx`:
       - Interface `ProgressBarProps { value: number }` (0-100)
       - Default export function
       - Clamp value: `const clamped = Math.min(100, Math.max(0, value))`
       - Render: track div with role="progressbar", aria-valuenow={clamped}, aria-valuemin={0}, aria-valuemax={100}
       - Inside: fill div with `style={{ width: \`${clamped}%\` }}` (this is the ONE exception to no-inline-styles — dynamic width requires it)

    5. Create `frontend/src/components/ProgressBar/ProgressBar.module.css`:
       - `.track`: width 100%, height 6px, border-radius 3px, background `var(--color-border)`, overflow hidden
       - `.fill`: height 100%, border-radius 3px, background `var(--color-accent)`, transition width `var(--transition)`

    6. Write tests following Login.test.tsx pattern (vitest + RTL, no providers needed for these pure components).
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Personales/tm-scorekeeper/frontend && npm test -- --run AchievementIcon ProgressBar</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "lucide-react" in dependencies
    - AchievementIcon.tsx contains `const ICON_MAP: Record<string, IconComponent>`
    - AchievementIcon.tsx contains `import { Trophy, Flame, Map, Gamepad2, Star, Zap } from 'lucide-react'`
    - AchievementIcon.tsx contains `ICON_MAP[fallback_icon] ?? Trophy`
    - AchievementIcon.module.css contains `.circle` with `border-radius: 50%`
    - AchievementIcon.module.css contains `.locked` with `grayscale(100%)` and `opacity: 0.5`
    - AchievementIcon.module.css contains `.unlocked` with `var(--color-accent)`
    - ProgressBar.tsx contains `role="progressbar"` and `aria-valuenow`
    - ProgressBar.tsx contains `Math.min(100, Math.max(0, value))`
    - ProgressBar.module.css contains `.track` with `height: 6px`
    - ProgressBar.module.css contains `.fill` with `var(--color-accent)`
    - AchievementIcon.test.tsx exits 0 with all tests passing
    - ProgressBar.test.tsx exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    lucide-react installed. AchievementIcon renders 6 mapped Lucide icons with Trophy fallback. ProgressBar renders 0-100% fill with clamped value. Both have passing unit tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build AchievementCard, AchievementBadgeMini, and TabBar</name>
  <files>
    frontend/src/components/AchievementCard/AchievementCard.tsx
    frontend/src/components/AchievementCard/AchievementCard.module.css
    frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx
    frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.module.css
    frontend/src/components/TabBar/TabBar.tsx
    frontend/src/components/TabBar/TabBar.module.css
    frontend/src/test/components/AchievementCard.test.tsx
    frontend/src/test/components/AchievementBadgeMini.test.tsx
    frontend/src/test/components/TabBar.test.tsx
  </files>
  <read_first>
    frontend/src/components/AchievementIcon/AchievementIcon.tsx
    frontend/src/components/ProgressBar/ProgressBar.tsx
    frontend/src/index.css
    frontend/src/types/index.ts
    frontend/src/components/Button/Button.tsx
    frontend/src/components/Button/Button.module.css
    frontend/src/test/components/Login.test.tsx
    .planning/phases/03-frontend/03-UI-SPEC.md
  </read_first>
  <behavior>
    AchievementCard:
    - Renders title text "Test Achievement"
    - Renders description text "Test description"
    - Renders AchievementIcon with correct fallback_icon and unlocked props
    - Shows "NIVEL 2" when tier=2 and unlocked=true
    - Shows "NIVEL 0" when unlocked=false
    - Shows ProgressBar with computed percentage when progress is not null
    - Shows counter "2/5" when progress={current:2, target:5}
    - Hides progress bar and counter when progress is null
    - Applies "unlocked" CSS class when unlocked=true
    - Applies "locked" CSS class when unlocked=false

    AchievementBadgeMini:
    - Renders title text
    - Renders AchievementIcon at size 20
    - Shows "NIVEL N" tier pill
    - Uses data-type="new" when is_new=true (border accent color)
    - Uses data-type="upgrade" when is_upgrade=true (border success color)

    TabBar:
    - Renders 3 tab buttons with text "Stats", "Records", "Logros"
    - Active tab has aria-selected="true"
    - Clicking inactive tab calls onTabChange with the tab id
    - Active tab has "active" CSS class
  </behavior>
  <action>
    1. Create `frontend/src/components/AchievementCard/AchievementCard.tsx`:
       - Interface `AchievementCardProps`:
         ```
         title: string
         description: string
         fallback_icon: string
         tier: number        // 0 when locked
         max_tier: number
         unlocked: boolean
         progress: { current: number; target: number } | null
         onClick?: () => void   // for catalog clickable cards
         ```
       - Default export function
       - Compute progress percentage: `progress ? Math.round((progress.current / progress.target) * 100) : 0` with guard for target===0 returning 0
       - Compute counter string: `progress ? \`${progress.current}/${progress.target}\` : undefined`
       - Layout per UI-SPEC:
         - Outer div: `styles.card` + (unlocked ? styles.unlocked : styles.locked), add onClick handler if provided, add role="button" + tabIndex={0} if onClick provided
         - Left: `<AchievementIcon fallback_icon={fallback_icon} size={24} unlocked={unlocked} />`
         - Right column: title (h3, 16px semibold), description (p, 14px muted)
         - Bottom row: NIVEL label (`<span className={styles.level}>NIVEL {unlocked ? tier : 0}</span>`), ProgressBar (only if progress !== null), counter span (only if progress !== null)

    2. Create `frontend/src/components/AchievementCard/AchievementCard.module.css`:
       - `.card`: display flex, flex-direction row, gap 12px, padding `var(--spacing-md)`, background `var(--color-surface)`, border 1px solid `var(--color-border)`, border-radius `var(--border-radius)`, cursor default
       - `.card[role="button"]`: cursor pointer
       - `.rightColumn`: flex 1, display flex, flex-direction column, gap `var(--spacing-xs)`
       - `.title`: font-size `var(--font-size-base)`, font-weight `var(--font-weight-semibold)`, color `var(--color-text)`, margin 0
       - `.description`: font-size `var(--font-size-sm)`, font-weight `var(--font-weight-normal)`, color `var(--color-text-muted)`, margin 0
       - `.bottomRow`: display flex, align-items center, gap `var(--spacing-sm)`, margin-top `var(--spacing-xs)`
       - `.level`: font-size 0.75rem, font-weight `var(--font-weight-semibold)`, text-transform uppercase, letter-spacing 0.05em, white-space nowrap
       - `.unlocked .level`: color `var(--color-accent)`
       - `.locked .level`: color `var(--color-text-muted)`
       - `.progressWrapper`: flex 1
       - `.counter`: font-size 0.75rem, font-weight `var(--font-weight-semibold)`, color `var(--color-text-muted)`, white-space nowrap

    3. Create `frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.tsx`:
       - Interface `AchievementBadgeMiniProps`:
         ```
         title: string
         tier: number
         fallback_icon: string
         is_new: boolean
         is_upgrade: boolean
         ```
       - Default export function
       - Determine type: `const badgeType = is_new ? 'new' : 'upgrade'`
       - Render outer div with `data-type={badgeType}`, className styles.badge
       - Inside: `<AchievementIcon fallback_icon={fallback_icon} size={20} unlocked={true} />`, title span, tier pill `<span className={styles.tierPill}>NIVEL {tier}</span>`

    4. Create `frontend/src/components/AchievementBadgeMini/AchievementBadgeMini.module.css`:
       - `.badge`: display flex, align-items center, gap `var(--spacing-sm)`, padding `var(--spacing-sm)` `var(--spacing-md)`, background `var(--color-surface)`, border-radius `var(--border-radius)`, border-left 3px solid transparent
       - `.badge[data-type="new"]`: border-left-color `var(--color-accent)`
       - `.badge[data-type="upgrade"]`: border-left-color `var(--color-success)`
       - `.badge[data-type="new"] .tierPill`: color `var(--color-accent)`
       - `.badge[data-type="upgrade"] .tierPill`: color `var(--color-success)`
       - `.title`: font-size `var(--font-size-sm)`, font-weight `var(--font-weight-semibold)`, color `var(--color-text)`
       - `.tierPill`: font-size 0.6875rem, font-weight `var(--font-weight-semibold)`, text-transform uppercase, letter-spacing 0.05em

       NOTE on CSS Modules + data-attributes: CSS Modules scopes class names but NOT data-attributes. Use `.badge` as the scoped selector combined with `:global([data-type="new"])` or use composes pattern. The simplest approach: use two CSS classes instead — `.badgeNew` and `.badgeUpgrade` — applied conditionally via className. The data-type attribute can remain for semantic purposes but styling via classes:
       - `.badgeNew`: border-left-color `var(--color-accent)`
       - `.badgeUpgrade`: border-left-color `var(--color-success)`
       - `.badgeNew .tierPill` won't work in CSS Modules (tierPill is hashed). Instead, use separate classes `.tierPillNew { color: var(--color-accent) }` and `.tierPillUpgrade { color: var(--color-success) }`.

    5. Create `frontend/src/components/TabBar/TabBar.tsx`:
       - Type `Tab = 'stats' | 'records' | 'logros'`
       - Interface `TabBarProps { activeTab: Tab; onTabChange: (tab: Tab) => void }`
       - Export the `Tab` type as named export
       - Default export function
       - Define tabs array: `[{ id: 'stats', label: 'Stats' }, { id: 'records', label: 'Records' }, { id: 'logros', label: 'Logros' }]`
       - Render nav with role="tablist", each tab as `<button role="tab" aria-selected={activeTab === tab.id} className={...} onClick={() => onTabChange(tab.id)}>{tab.label}</button>`

    6. Create `frontend/src/components/TabBar/TabBar.module.css`:
       - `.tabBar`: display flex, width 100%, background `var(--color-surface)`, border-bottom 1px solid `var(--color-border)`
       - `.tab`: flex 1, height 44px, border none, background transparent, color `var(--color-text-muted)`, font-size `var(--font-size-sm)`, font-weight `var(--font-weight-normal)`, text-transform uppercase, letter-spacing 0.05em, cursor pointer, border-bottom 2px solid transparent, transition color `var(--transition)`
       - `.active`: color `var(--color-text)`, font-weight `var(--font-weight-semibold)`, border-bottom-color `var(--color-accent)`

    7. Write tests for all 3 components following Login.test.tsx pattern. For components that import AchievementIcon (which imports from lucide-react), the tests should render fine since lucide-react renders SVGs.
  </action>
  <verify>
    <automated>cd /Users/facu/Desarrollos/Personales/tm-scorekeeper/frontend && npm test -- --run AchievementCard AchievementBadgeMini TabBar</automated>
  </verify>
  <acceptance_criteria>
    - AchievementCard.tsx contains `import AchievementIcon from '@/components/AchievementIcon/AchievementIcon'`
    - AchievementCard.tsx contains `import ProgressBar from '@/components/ProgressBar/ProgressBar'`
    - AchievementCard.tsx contains `NIVEL ${unlocked ? tier : 0}` or equivalent template literal
    - AchievementCard.tsx contains `progress.current / progress.target` percentage computation
    - AchievementCard.module.css contains `.level` with `text-transform: uppercase`
    - AchievementCard.module.css contains `.card` with `var(--color-surface)`
    - AchievementBadgeMini.tsx contains `import AchievementIcon from '@/components/AchievementIcon/AchievementIcon'`
    - AchievementBadgeMini.tsx contains `size={20}` prop on AchievementIcon
    - AchievementBadgeMini.module.css contains `border-left` with `3px solid`
    - AchievementBadgeMini.module.css contains `var(--color-accent)` and `var(--color-success)`
    - TabBar.tsx contains `role="tablist"` and `role="tab"` and `aria-selected`
    - TabBar.tsx exports `type Tab = 'stats' | 'records' | 'logros'`
    - TabBar.module.css contains `.tab` with `height: 44px`
    - TabBar.module.css contains `.active` with `var(--color-accent)`
    - All 3 test files exit 0
  </acceptance_criteria>
  <done>
    AchievementCard renders full achievement layout with icon, title, description, NIVEL label, progress bar, and counter. Locked/unlocked states work correctly. AchievementBadgeMini renders mini badge with accent/success border differentiation. TabBar renders 3 tabs with active state. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm test -- --run` — all new component tests pass
- `cd frontend && npx tsc --noEmit` — no TypeScript errors
- lucide-react appears in package.json dependencies
- All 5 component directories exist under frontend/src/components/
</verification>

<success_criteria>
All 5 leaf components (AchievementIcon, ProgressBar, AchievementCard, AchievementBadgeMini, TabBar) built with CSS Modules and tested. lucide-react installed. Components ready for consumption by plans 02 and 03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend/03-01-SUMMARY.md`
</output>
